# Source Plugin And Roadmap Placement

## Arena As Future Source Adapter Family

Future adapter:

```txt
Cyber Arena Adapter
→ reads arena.graph.json
→ reads match timeline/replay
→ reads agent reasoning traces / observable artifacts
→ builds interactive graph
→ recommends arena visual dialect
→ routes match events through Signal Loom
→ produces evidence/scoring panels
```

## Relationship To Existing Roadmap

Recommended prerequisite sequence:

```txt
v70 QA Bundle Validator
→ v71 Contract-to-Code Trace Matrix
→ v72 Registry Explorer / Searchable System Index
→ v74 Source Adapter OS Reconnect Contract
→ v75 Synthetic Data Fixtures v0
→ Visual Grammar Engine
→ Signal Loom
→ Replay Timeline
→ Arena Fixture Generator
→ Agent Tournament Harness
```

## Future Epic

```txt
Future Epic: LumaWeave Arena
```

## Future Submodules

- ArenaGrid — procedural graph arena generator
- Model Coliseum — LLM-vs-LLM benchmark/tournament mode
- Security Sentinel Arena — defensive project hardening simulations
- Replay Atlas — timeline and evidence replay
- Signal Loom Spectator Mode — live event visual routing

## MVP Concept

First non-runtime packet should be docs-only:

```txt
LumaWeave Arena Contract
Arena Graph Fixture Schema
Arena Safety Boundary
Arena Scoring Model
Arena Replay Format
```

First implementation should be synthetic fixture-only and read-only.

## Do Not Implement Yet

This packet is not authorization to build arena runtime behavior.

Do not add:

- live model orchestration
- exploit simulation runtime
- network scanning
- command execution
- real target testing
- Signal Loom runtime effects
- graph/Sigma mutation

without explicit future contracts and safety gates.
