# Known Sharp Edges

Project-specific behaviors that have caught us before. Honor the workarounds. (Live bug list is in `docs/LUMAWEAVE_NOW.md` / `docs/known-bugs/`; this is the stable set of recurring traps.)

## Stale closure in event-handler setup

`CollapsibleSection`'s tear-off handler creates an `onMove` closure in `pointerdown` that captures registry callbacks at that moment. If a callback then reads state from `useState` or a `useCallback` dep array, it sees stale state.

Fix pattern: registry callbacks read current state via `useSettingsStore.getState()` synchronously. Don't rely on `useState`+`useEffect` syncing for callbacks invoked from captured event-handler closures. (This same class of bug — closures reading render-snapshot data instead of live store — has appeared in tile drag handlers too; read live, not the captured snapshot.)

## Playwright + browser cache

The `~/.cache/ms-playwright/` Chromium binary occasionally gets evicted. `scripts/ensure-playwright-browser.mjs` auto-reinstalls if missing; `npm run qa:e2e` self-heals silently. (Browsers are pinned at `$HOME/pw-browsers` via `PLAYWRIGHT_BROWSERS_PATH` — don't reinstall manually.)

## Self-graph fixture

`src/fixtures/self-graph-generated.json` is auto-generated by `scripts/generate-self-graph.mjs` (a Vite plugin self-heals it on startup if missing). It's gitignored — don't commit it. CI must run `npm run generate:graph` before typecheck since the clean checkout won't have it.

## Known test flakes

- **Gwells C9.0 drift-back** — `tests/e2e/gwells-physics.spec.ts` ("drifts back toward seed") occasionally fails under full-suite load; passes in isolation. Doc: `docs/known-bugs/gwells-c9-0-drift-back-flake.md`. If it fails, re-run gwells-physics.spec.ts alone; passing there confirms the flake.
- **Contract registry timeout** — `tests/e2e/contract-registry.spec.ts` ("v48 checklist includes detail mode checks") times out at 30s on `qa-check-previous`. Doc: `docs/known-bugs/contract-registry-qa-check-previous-timeout.md`. Known flaky helper; expected if it fires.

## Zustand selectors: never construct new object references inline as fallbacks

**Symptom:** Component enters an infinite re-render loop when reading optional config from the settings store.

**Cause:** A selector that returns a freshly-constructed object (or array) when the underlying value is `undefined` produces a new reference on every call. Zustand's default equality check (`Object.is`) treats each new reference as a state change → re-render → selector re-runs → new reference → re-render → loop.

**Anti-pattern:**
```typescript
const config = useSettingsStore(
  (s) => s.settings.sources.configurations[adapterId] ?? { adapterId },
);
```
The `?? { adapterId }` fallback creates a new object every call. Infinite loop.

**Correct pattern:** keep the selector pure (return the actual stored value or `undefined`), apply the fallback outside the selector:
```typescript
const storedConfig = useSettingsStore(
  (s) => s.settings.sources.configurations[adapterId],
);
const config = storedConfig ?? { adapterId };
```
The selector returns `undefined` consistently (same reference) when the config is missing. The render-body fallback is fine — React doesn't re-render based on render-body object identity.

**First encountered:** v109.0.4 (`AdapterConfigForm.tsx`). Manifested as a frozen browser tab; typecheck and Vite build both passed clean (purely a runtime React loop).

**Related patterns:** any Zustand selector returning an aggregated value — e.g. `(s) => s.items.filter(...)` — the filter result is a new array every call. Same fix: select the raw state, derive in the component body.
